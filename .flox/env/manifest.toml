schema-version = "1.14.0"
# Generated by /floxify

[install]
# docker-compose has no x86_64-darwin build (see `flox show docker-compose`);
# scoped to the platforms the catalog actually ships.
docker-compose.pkg-path = "docker-compose"
docker-compose.systems = ["aarch64-darwin", "aarch64-linux", "x86_64-linux"]
